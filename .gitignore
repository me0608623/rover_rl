# Build artifacts
build/
install/
log/
.director-mode/
*.egg-info/
__pycache__/
*.pyc
*.pyo

# Editor
.vscode/
.idea/
*.swp
*.swo

# 本地手動備份（改動前的 .bak 安全副本；git 歷史已保留原版，不入庫）
*.bak_*

# Models（不 commit 大檔，由 Release 或 LFS 管理；可單獨開白名單）
models/*.ts
models/*.pt
!models/.gitkeep

# 診斷記錄（diag_logger 產出，實驗資料不入庫）
logs/
wandb/

# rosbag（離線測試感測器錄製，大檔不入庫）
src/bag/

# OS
.DS_Store
Thumbs.db
